import React from "react";
import "../scss/MainScreen.scss"; // Asegúrate de tener un archivo SCSS asociado

const MainScreen = () => {
  return (
    <div className="main-screen">
      <h1>Bienvenido a Almacafé</h1>
      <p>Usa el menú lateral para navegar entre las diferentes opciones.</p>
    </div>
  );
};

export default MainScreen;
